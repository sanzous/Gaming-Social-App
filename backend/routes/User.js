const express = require("express");
const router = express.Router();
const {} = require("../controllers/User");

/* --- /api/users --- */

// router.route("/").get(controllerFunction).post(controllerFunction)

module.exports = router;